blank_issues_enabled: true
contact_links:
  - name: Questions about vLLM itself
    url: https://github.com/vllm-project/vllm/issues
    about: This fork changes only the accumulation inside quantized linear layers. Anything else is upstream's.
